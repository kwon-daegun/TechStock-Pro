<Window x:Class="InventorySystem.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TechStock Login" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#0C0F14" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#161921"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#161921"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#5EB8FA"/>
            <Setter Property="Foreground" Value="#000"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                                <Setter Property="Foreground" Value="#FFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#161921">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Background="#1E263C" Width="80" Height="80" CornerRadius="20" Margin="0,0,0,20">
                    <Image Source="../Assets/Images/Logo/logo.png" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform"/>
                </Border>
                <TextBlock Text="TechStock Pro" FontSize="32" FontWeight="Bold" Foreground="#FFF" HorizontalAlignment="Center"/>
                <TextBlock Text="Store Management System" FontSize="16" Foreground="#9CA3AF" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="40">
            <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" Foreground="#FFF" Margin="0,0,0,0"/>
            <TextBlock Text="Please enter your details to sign in." FontSize="14" Foreground="#9CA3AF" Margin="0,0,0,50"/>

            <TextBlock Text="Username" Foreground="#D1D5DB" Margin="0,0,0,5"/>
            <TextBox x:Name="txtUsername" Height="45" Margin="0,0,0,20"/>

            <TextBlock Text="Password" Foreground="#D1D5DB" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Height="45" Margin="0,0,0,30"/>

            <Button Content="Sign In" Height="50" Click="LoginButton_Click" FontWeight="Bold" FontSize="16" Margin="0,40,0,0" Cursor="Hand"/>

            <Button Content="Exit App" Height="50" Background="Transparent" Foreground="#6B7280" Margin="0,10,0,0" Click="Exit_Click" FontWeight="SemiBold" FontSize="14" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>