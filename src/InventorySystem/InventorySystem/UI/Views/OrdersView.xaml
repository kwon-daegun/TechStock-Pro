<UserControl x:Class="InventorySystem.UI.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#111827" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <Border.Effect>
                                <DropShadowEffect Color="#2563EB" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="MinimalComboBoxTemplate" TargetType="ComboBox">
            <Grid>
                <ToggleButton Focusable="false" 
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                          ClickMode="Press"
                          BorderThickness="0"
                          Background="Transparent">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border Background="Transparent">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="1" Text="▼" FontSize="10" Foreground="#9CA3AF" 
                                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <ContentPresenter IsHitTestVisible="False" 
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Left"
                              Margin="0,0,20,0"/>

                <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}" 
                           AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                    <Border x:Name="DropDownBorder" Background="#1F2937" 
                                BorderThickness="1" BorderBrush="#374151" 
                                CornerRadius="6" MinWidth="{TemplateBinding ActualWidth}">
                        <ScrollViewer Margin="2">
                            <StackPanel IsItemsHost="True" />
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </ControlTemplate>

    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="Orders History" Foreground="#FFF" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="View and manage past sales transactions" Foreground="#9CA3AF" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#1F2937" CornerRadius="8" Padding="10,5" Width="300">
                <DockPanel>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#9CA3AF"/>
                    <TextBox x:Name="TxtSearchOrder"
                                 Text="Search Order ID..." 
                                 TextChanged="TxtSearchOrder_TextChanged"
                                 Foreground="#9CA3AF"
                                 Background="Transparent"
                                 BorderThickness="0" 
                                 CaretBrush="#FFF"
                                 VerticalContentAlignment="Center"
                                 GotFocus="RemovePlaceholder"
                                 LostFocus="AddPlaceholder"/>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Background="#1F2937" CornerRadius="8" Padding="10,0" Height="38" Margin="15,0,0,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Date: " Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="CmbDateFilter" Width="120" SelectionChanged="CmbDateFilter_SelectionChanged"
                              Template="{StaticResource MinimalComboBoxTemplate}" Foreground="White" 
                              Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"
                              FontWeight="SemiBold" SelectedIndex="0">
                        <ComboBox.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#1F2937"/>
                        </ComboBox.Resources>
                        <ComboBoxItem Content="All Time"/>
                        <ComboBoxItem Content="Today"/>
                        <ComboBoxItem Content="Last 7 Days"/>
                        <ComboBoxItem Content="Last 30 Days"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <Button Click="BtnNewOrder_Click" Grid.Column="4" Style="{StaticResource PrimaryButtonStyle}" Content="+ New Order" Width="150"/>
        </Grid>

        <Border Grid.Row="2" Background="#1F2937" CornerRadius="16" Padding="5">
            <DataGrid x:Name="OrdersGrid"
                      Background="Transparent" BorderThickness="0" RowBackground="Transparent"
                      Foreground="#FFF" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#374151"
                      HeadersVisibility="Column" AutoGenerateColumns="False" RowHeight="60"
                      CanUserAddRows="False" CanUserSortColumns="False" CanUserResizeColumns="False" 
                      IsReadOnly="True" SelectionMode="Single"
                      ScrollViewer.HorizontalScrollBarVisibility="Hidden">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#9CA3AF"/>
                        <Setter Property="Padding" Value="15"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#374151"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderThickness="0" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center" Margin="15,0"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#374151"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="ORDER ID" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Id, StringFormat='#{0:D5}'}" IsReadOnly="True" 
                                         Background="Transparent" Foreground="#fff" BorderThickness="0" 
                                         FontFamily="Consolas" FontWeight="Bold" VerticalAlignment="Center" 
                                         Cursor="IBeam"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="DATE" Binding="{Binding OrderDate, StringFormat='MMM dd, yyyy'}" Width="*" Foreground="#D1D5DB"/>

                    <DataGridTemplateColumn Header="ITEMS" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ItemsCount, StringFormat='{}{0} items'}" Foreground="#9CA3AF"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="TOTAL" Binding="{Binding TotalAmount, StringFormat='₹ {0:N2}'}" Width="*" FontWeight="Bold" Foreground="#FFF"/>

                    <DataGridTemplateColumn Header="" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Click="BtnViewOrder_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <TextBlock Text="👁‍🗨" FontSize="16" Foreground="#9CA3AF" ToolTip="View Details"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn x:Name="ColDelete" Header="ACTIONS" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑" Click="BtnDeleteOrder_Click" Width="30" 
                                        Height="30" Background="Transparent" Foreground="#EF4444"
                                        BorderThickness="0" Cursor="Hand"
                                        ToolTip="Delete Order And Restore Items to Stock"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,5">
                <TextBlock Text="Showing 1-10 of 50" VerticalAlignment="Center" Foreground="Gray" 
                       Margin="0,0,20,0" x:Name="TxtPageInfo"/>
                <Button x:Name="BtnPrevious" Content="&lt;" Width="30" Height="30" Click="BtnPrevious_Click" 
                    Background="#1F2937" Foreground="#FFF" BorderThickness="0" Margin="2">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Border Background="#3B82F6" CornerRadius="4" Width="30" Height="30" Margin="2">
                    <TextBlock x:Name="TxtCurrentPage" Text="1" Foreground="#FFF" HorizontalAlignment="Center"
                           VerticalAlignment="Center" FontWeight="Bold"/>
                </Border>
                <Button x:Name="BtnNext" Content="&gt;" Width="30" Height="30" Click="BtnNext_Click" 
                    Background="#1F2937" Foreground="#FFF" BorderThickness="0" Margin="2">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>