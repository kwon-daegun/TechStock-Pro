<Window x:Class="InventorySystem.UI.Views.Windows.SupplierFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supplier Details" Height="650" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#111827">

    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                    Background="{TemplateBinding Background}" 
                    CornerRadius="6" 
                    Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                                <Setter Property="Foreground" Value="#FFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="20" Padding="10">
        <StackPanel>
            <TextBlock x:Name="TxtTitle" Text="New Supplier" FontSize="24" FontWeight="Bold" Foreground="#FFF" Margin="0,0,0,20"/>

            <TextBlock Text="Company Name" Foreground="#9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="InputName" VerticalAlignment="Center" Height="35" FontSize="14" Padding="5,5,0,0" Background="#1F2937" Foreground="#fff" BorderThickness="1" BorderBrush="#374151"/>

            <TextBlock Text="Contact Person" Foreground="#9CA3AF" Margin="0,15,0,5"/>
            <TextBox x:Name="InputContact" VerticalAlignment="Center" Padding="5,5,0,0" Height="35" FontSize="14" Background="#1F2937" Foreground="#fff" BorderThickness="1" BorderBrush="#374151"/>

            <Grid Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Email" Foreground="#9CA3AF" Margin="0,0,0,5"/>
                    <TextBox x:Name="InputEmail" Height="35" FontSize="14" VerticalAlignment="Center" Padding="5,5,0,0" Background="#1F2937" Foreground="#FFF" BorderThickness="1" BorderBrush="#374151"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Phone" Foreground="#9CA3AF" Margin="0,0,0,5"/>
                    <TextBox x:Name="InputPhone" Height="35" FontSize="14" VerticalAlignment="Center" Padding="5,5,0,0" Background="#1F2937" Foreground="#fff" BorderThickness="1" BorderBrush="#374151"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Address" Foreground="#9CA3AF" Margin="0,15,0,5"/>
            <TextBox x:Name="InputAddress" Height="60" FontSize="14" VerticalAlignment="Center" Padding="5,5,0,0" 
                     TextWrapping="Wrap" AcceptsReturn="True" Background="#1F2937" Foreground="#FFF" 
                     BorderThickness="1" BorderBrush="#374151" VerticalContentAlignment="Top"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="Cancel" Style="{StaticResource GhostButtonStyle}" IsCancel="True" Click="BtnCancel_Click" Width="100" Height="40" Background="Transparent" Foreground="#9CA3AF" BorderBrush="#4B5563" Margin="0,0,10,0"/>
                <Button Content="Save Supplier" Style="{StaticResource PrimaryButtonStyle}"  Click="BtnSave_Click" Width="140" Height="40" Background="#2563EB" Foreground="#fff" FontWeight="Bold" BorderThickness="0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>