<Window x:Class="InventorySystem.UI.Views.Windows.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New User" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#111827">

    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                                <Setter Property="Foreground" Value="#FFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1F2937"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2563EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="30">
        <StackPanel>
            <TextBlock Text="Create User" FontSize="24" FontWeight="Bold" Foreground="#fff" Margin="0,0,0,25"/>

            <TextBlock Text="Username" Foreground="#9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtUsername" Height="40" FontSize="14" Padding="10,0" VerticalContentAlignment="Center"
                     Background="#1F2937" Foreground="#fff" BorderThickness="1" BorderBrush="#374151"/>

            <TextBlock Text="Password" Foreground="#9CA3AF" Margin="0,15,0,5"/>
            <PasswordBox x:Name="TxtPassword" Height="40" FontSize="14" Padding="10,0" VerticalContentAlignment="Center"
                         Background="#1F2937" Foreground="#fff" BorderThickness="1" BorderBrush="#374151"/>

            <TextBlock Text="Role" Foreground="#9CA3AF" Margin="0,15,0,5"/>
            <ComboBox x:Name="CmbRole" Height="40" FontSize="14" Padding="10,0" VerticalContentAlignment="Center"
                      Background="#fff" Foreground="#000" BorderThickness="1" BorderBrush="#374151">
                <ComboBoxItem Content="Staff" IsSelected="True"/>
                <ComboBoxItem Content="Admin"/>
            </ComboBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="Cancel" Style="{StaticResource GhostButtonStyle}" Click="BtnCancel_Click" Width="100" Height="40" 
                        Background="Transparent" Foreground="#9CA3AF" BorderBrush="#4B5563" Margin="0,0,10,0"/>

                <Button Content="Save User" Style="{StaticResource PrimaryButtonStyle}" Click="BtnSave_Click" Width="120" Height="40" 
                        Background="#2563EB" Foreground="#fff" FontWeight="Bold" BorderThickness="0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>