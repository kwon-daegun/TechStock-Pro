<UserControl x:Class="InventorySystem.UI.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#111827" FontFamily="Segoe UI">

    <UserControl.Resources>
       
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1F2937"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <Grid>
                                <TextBlock x:Name="Placeholder" 
                                           Text="🔍 Search products..." 
                                           Foreground="#6B7280" 
                                           Margin="15,10" 
                                           Visibility="Hidden"/>

                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <Border.Effect>
                                <DropShadowEffect Color="#2563EB" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Product Inventory" Foreground="#FFF" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Manage your store catalog and stock levels" Foreground="#9CA3AF" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>

            <Button Click="BtnAdd_Click" Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}" Content="+ Add New Product"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Style="{StaticResource SearchBoxStyle}"/>

            <Border Grid.Column="2" Background="#1F2937" CornerRadius="8" Padding="15,10" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Filter by: " Foreground="#9CA3AF"/>
                    <TextBlock Text="All Categories" Foreground="#FFF" FontWeight="SemiBold"/>
                    <TextBlock Text=" ▼" Foreground="#9CA3AF" FontSize="10" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#1F2937" CornerRadius="16" Padding="5">
            <DataGrid x:Name="ProductsGrid"
                      Background="Transparent"
                      BorderThickness="0"
                      RowBackground="Transparent"
                      Foreground="#FFF"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#374151"
                      HeadersVisibility="Column"
                      AutoGenerateColumns="False"
                      RowHeight="70"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                      MouseDoubleClick="ProductsGrid_MouseDoubleClick">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#9CA3AF"/>
                        <Setter Property="Padding" Value="15"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#374151"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderThickness="0" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center" Margin="15,0"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#374151"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="IMAGE" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="45" Height="45" CornerRadius="8" Background="#111827" ClipToBounds="True">
                                    <Image Source="{Binding ImageUrl}" Stretch="UniformToFill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="PRODUCT" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#FFF"/>
                                    <TextBlock Text="{Binding Brand}" FontSize="12" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="*" Foreground="#D1D5DB"/>

                    <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat='₹ {0:N2}'}" Width="*" FontWeight="Bold" Foreground="#FFF"/>

                    <DataGridTemplateColumn Header="STOCK LEVEL" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Stock}" FontWeight="Bold" Foreground="#FFF"/>
                                        <TextBlock Text=" items" Foreground="#6B7280" FontSize="11" Margin="5,2,0,0"/>
                                    </StackPanel>
                                    <ProgressBar Value="{Binding Stock}" Maximum="100" Height="4" BorderThickness="0"
                                                 Background="#374151">
                                        <ProgressBar.Foreground>
                                            <SolidColorBrush Color="{Binding StockLevelClr}"/>
                                        </ProgressBar.Foreground>
                                    </ProgressBar>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="STATUS" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding StatusBgClr}" CornerRadius="12" Padding="12,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusTextClr}" FontSize="11" FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,0,8">
                <TextBlock Text="Showing 1-10 of 50" VerticalAlignment="Center" Foreground="Gray" 
                           Margin="0,0,20,0" x:Name="TxtPageInfo"/>
                <Button x:Name="BtnPrevious" Content="&lt;" Width="30" Height="30" Click="BtnPrevious_Click" 
                        Background="#1F2937" Foreground="#FFF" BorderThickness="0" Margin="2">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Border Background="#3B82F6" CornerRadius="4" Width="30" Height="30" Margin="2">
                    <TextBlock x:Name="TxtCurrentPage" Text="1" Foreground="#FFF" HorizontalAlignment="Center"
                               VerticalAlignment="Center" FontWeight="Bold"/>
                </Border>
                <Button x:Name="BtnNext" Content="&gt;" Width="30" Height="30" Click="BtnNext_Click"
                        Background="#1F2937" Foreground="#FFF" BorderThickness="0" Margin="2">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>