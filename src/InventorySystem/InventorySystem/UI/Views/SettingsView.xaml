<UserControl x:Class="InventorySystem.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#111827" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#1F2937"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Manage your profile and system users." FontSize="14" Foreground="#9CA3AF" Margin="0,5,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Padding="20,10" Background="Transparent" CornerRadius="6,6,0,0" Margin="0,0,5,0">
                                    <ContentPresenter ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#2563EB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#1F2937"/>
                                        <Setter Property="Foreground" Value="#9CA3AF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Style>
            </TabControl.Resources>

            <TabItem Header="My Profile">
                <Border Background="#1F2937" CornerRadius="0,8,8,8" Padding="40">
                    <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                        <TextBlock Text="Change Password" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,25"/>

                        <TextBlock Text="Current Password" Foreground="#9CA3AF" Margin="0,0,0,8"/>
                        <PasswordBox x:Name="TxtOldPass"/>

                        <TextBlock Text="New Password" Foreground="#9CA3AF" Margin="0,20,0,8"/>
                        <PasswordBox x:Name="TxtNewPass"/>

                        <TextBlock Text="Confirm New Password" Foreground="#9CA3AF" Margin="0,20,0,8"/>
                        <PasswordBox x:Name="TxtConfirmPass"/>

                        <Button Content="Update Password" Click="BtnUpdatePassword_Click"
                                Style="{StaticResource PrimaryButtonStyle}" Margin="0,30,0,0" Height="45" Background="#10B981"/>
                    </StackPanel>
                </Border>
            </TabItem>

            <TabItem Header="User Management" x:Name="TabUsers">
                <Border Background="#1F2937" CornerRadius="0,8,8,8" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DataGrid x:Name="UsersGrid" Grid.Row="1"
                                  Background="#111827" BorderThickness="0" RowBackground="Transparent"
                                  HeadersVisibility="Column" AutoGenerateColumns="False" RowHeight="50" IsReadOnly="True"
                                  HorizontalGridLinesBrush="#374151" GridLinesVisibility="Horizontal" CanUserAddRows="False">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#9CA3AF"/>
                                    <Setter Property="Padding" Value="15,0"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#374151"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Background="{TemplateBinding Background}" Padding="15,0">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#374151"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" Foreground="#6B7280"/>
                                <DataGridTextColumn Header="USERNAME" Binding="{Binding Username}" Width="*" FontWeight="Bold"/>
                                <DataGridTextColumn Header="ROLE" Binding="{Binding Role}" Width="150"/>

                                <DataGridTemplateColumn Header="ACTIONS" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="🔑" Click="BtnResetPass_Click" Width="35" Height="30"
                                                        Background="#1F2937" Foreground="#FBBF24" BorderBrush="#FBBF24" BorderThickness="1"
                                                        Cursor="Hand" ToolTip="Reset Password to '1234'">
                                                    <Button.Resources>
                                                        <Style TargetType="Border">
                                                            <Setter Property="CornerRadius" Value="4"/>
                                                        </Style>
                                                    </Button.Resources>
                                                </Button>

                                                <Button Content="🗑" Click="BtnDeleteUser_Click" Width="35" Height="30"
                                                        Background="#1F2937" Foreground="#EF4444" BorderBrush="#EF4444" BorderThickness="1"
                                                        Cursor="Hand" Margin="10,0,0,0" ToolTip="Delete User">
                                                    <Button.Resources>
                                                        <Style TargetType="Border">
                                                            <Setter Property="CornerRadius" Value="4"/>
                                                        </Style>
                                                    </Button.Resources>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>

        <Button Grid.Row="1" x:Name="BtnAddUser"
                Content="+ Add New User" Click="BtnAddUser_Click"  
                Width="150" Height="40"  
                Style="{StaticResource PrimaryButtonStyle}" 
                HorizontalAlignment="Right" VerticalAlignment="Top" 
                Margin="0,0,0,0"/>

    </Grid>
</UserControl>